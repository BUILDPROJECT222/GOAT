node_modules
dist
.DS_Store
*.local
.vite

# env — jangan commit key; .env.example tetap di-track
.env
.env.*
!.env.example

# logs
*.log

# Separate standalone app with its own repo (BUILDPROJECT222/airdrop)
/airdrop/
